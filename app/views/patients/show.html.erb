<h1><%= @patient.name %></h1>

<% @patient.appointments.each do |appt| %>
<h3><%= appt.appointment_datetime.strftime("%B %d, %Y at %-H:%M") %></h3>
<h4><%= link_to appt.doctor.name, doctor_path(appt.doctor) %></h4>
<% end %>
