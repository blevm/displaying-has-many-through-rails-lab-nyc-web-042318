<h1><%= @doctor.name %></h1>
<h2><%= @doctor.department %></h2><br>

<% @doctor.appointments.each do |appt| %>
<h3><%= appt.appointment_datetime.strftime("%B %d, %Y at %-H:%M") %></h3>
<h4><%= link_to appt.patient.name, patient_path(appt.patient) %></h4>
<% end %>
